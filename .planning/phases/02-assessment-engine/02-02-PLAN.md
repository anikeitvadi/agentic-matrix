---
phase: 02-assessment-engine
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/assessment/page.tsx
  - app/assessment/components/AssessmentForm.tsx
  - app/assessment/components/StepIndicator.tsx
  - app/assessment/components/QuestionStep.tsx
  - app/assessment/steps/index.ts
  - app/assessment/steps/step-01-basics.tsx
  - app/assessment/steps/step-02-current-state.tsx
  - app/assessment/steps/step-03-requirements.tsx
  - app/assessment/steps/step-04-constraints.tsx
  - components/ui/Sidebar.tsx
autonomous: true

must_haves:
  truths:
    - "User can navigate to /assessment and see the questionnaire"
    - "User can fill in questions and progress through steps"
    - "User can navigate backward to review previous answers"
    - "Step indicator shows current position in the flow"
  artifacts:
    - path: "app/assessment/page.tsx"
      provides: "Assessment entry point (Server Component)"
      min_lines: 10
    - path: "app/assessment/components/AssessmentForm.tsx"
      provides: "Main form container with step management"
      contains: "useForm"
    - path: "app/assessment/components/StepIndicator.tsx"
      provides: "Progress tracker UI"
      contains: "aria-label"
    - path: "app/assessment/steps/index.ts"
      provides: "Step registry"
      exports: ["steps", "getStepSchema"]
  key_links:
    - from: "app/assessment/components/AssessmentForm.tsx"
      to: "react-hook-form"
      via: "useForm hook"
      pattern: "useForm<"
    - from: "app/assessment/components/AssessmentForm.tsx"
      to: "app/assessment/schemas/step-schemas.ts"
      via: "zodResolver import"
      pattern: "zodResolver"
    - from: "app/assessment/steps/index.ts"
      to: "app/assessment/steps/step-01-basics.tsx"
      via: "step component imports"
      pattern: "Step01Basics"
---

<objective>
Build the multi-step assessment form UI with React Hook Form integration and step navigation.

Purpose: Create the core questionnaire interface that users interact with to complete their assessment.
Output: Functional multi-step form at /assessment with forward/backward navigation and step progress indicator.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-assessment-engine/02-RESEARCH.md
@.planning/phases/02-assessment-engine/02-01-SUMMARY.md

Key patterns from research:
- Use `useWatch()` not `watch()` for React 19 compatibility
- Use `mode: 'onBlur'` for validation timing
- Allow free backward navigation (no validation), validate only on forward movement
- Step indicator should use `<nav>` with `aria-label="Progress"` for accessibility
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create assessment page and form container</name>
  <files>
    app/assessment/page.tsx
    app/assessment/components/AssessmentForm.tsx
    app/assessment/components/StepIndicator.tsx
  </files>
  <action>
Create directory structure:
```
app/assessment/
  page.tsx
  components/
    AssessmentForm.tsx
    StepIndicator.tsx
```

**app/assessment/page.tsx** (Server Component):
- Import and render AssessmentForm
- Page title: "Assessment" or "Platform Assessment"
- Simple container with consistent styling (match existing tool-like UI)

**app/assessment/components/AssessmentForm.tsx** (Client Component):
- Use 'use client' directive at top
- Import useForm from react-hook-form
- Import zodResolver from @hookform/resolvers/zod
- Import step schemas from ../schemas/step-schemas
- State: currentStep (number, starting at 1)
- Total steps: 4 (from schema design)

Form setup:
```typescript
const { register, handleSubmit, control, formState: { errors }, trigger } = useForm<AssessmentData>({
  resolver: zodResolver(getCurrentStepSchema(currentStep)),
  mode: 'onBlur',
})
```

Navigation handlers:
- `handleNext`: Validate current step with `trigger()`, if valid increment step
- `handleBack`: Decrement step (no validation needed)
- `handleSubmit`: Final submission (placeholder for now - Phase 3 will use this)

Render:
- StepIndicator at top
- Current step component in middle
- Navigation buttons at bottom (Back disabled on step 1, Next/Submit based on step)

**app/assessment/components/StepIndicator.tsx**:
- Props: current (number), total (number), stepLabels (string[])
- Accessible: `<nav aria-label="Progress">`
- Show step numbers with visual states: complete, current, incomplete
- Use Tailwind for styling (match existing dark sidebar aesthetic)
- Horizontal layout with connecting lines between steps
  </action>
  <verify>
Run dev server: `npm run dev`
Navigate to http://localhost:3000/assessment
Verify: Page loads, form renders, step indicator shows
  </verify>
  <done>
- /assessment route accessible
- Form container renders with useForm configured
- Step indicator shows 4 steps with current step highlighted
- No console errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Create step components and step registry</name>
  <files>
    app/assessment/steps/index.ts
    app/assessment/steps/step-01-basics.tsx
    app/assessment/steps/step-02-current-state.tsx
    app/assessment/steps/step-03-requirements.tsx
    app/assessment/steps/step-04-constraints.tsx
    app/assessment/components/QuestionStep.tsx
  </files>
  <action>
Create step components that render the questions for each step.

**app/assessment/components/QuestionStep.tsx**:
Reusable component for rendering different field types:
- Props: question (Question type), register, errors, control
- Render appropriate input based on fieldType:
  - text: `<input type="text" />`
  - textarea: `<textarea />`
  - select: `<select>` with options
  - multi-select: Checkbox group
  - radio: Radio button group
  - scale: Range input or button group (1-5 or 1-10)
  - yes-no: Two buttons or radio
- Show validation errors below each field
- Use Tailwind styling consistent with existing UI

**Step components** (step-01-basics.tsx through step-04-constraints.tsx):
Each step component:
- Props: register, errors, control (passed from AssessmentForm)
- Renders 3-5 questions using QuestionStep
- Step title and description at top
- Questions laid out vertically with spacing

Step 1 - Basics:
- "What brings you here today?" (useCase - radio)
- "How large is your organization?" (organizationSize - select)
- "What industry are you in?" (industry - text, optional)

Step 2 - Current State:
- "Do you have an existing AI agent platform?" (hasExistingPlatform - yes-no)
- "Which platforms are you currently using?" (existingPlatforms - multi-select, conditional)
- "What's your current tech stack?" (currentStack - multi-select)
- "What integrations do you need?" (integrationNeeds - multi-select)

Step 3 - Requirements:
- "What are your primary use cases?" (primaryUseCases - multi-select)
- "What compliance requirements do you have?" (complianceRequirements - multi-select)
- "What's your timeline?" (timeline - radio)

Step 4 - Constraints:
- "What's your budget range?" (budgetRange - radio)
- "What's your team's technical level?" (teamTechnicalLevel - radio)
- "Who are the decision makers?" (decisionMakers - multi-select)

**app/assessment/steps/index.ts**:
Step registry that exports:
- `steps`: Array of step configs (id, title, description, component)
- `getStepSchema(stepNumber)`: Returns the Zod schema for that step
- `stepLabels`: Array of step titles for StepIndicator
  </action>
  <verify>
Run dev server and test navigation:
1. Go to /assessment
2. Fill in Step 1 fields
3. Click Next - should advance to Step 2
4. Click Back - should return to Step 1 with values preserved
5. Navigate through all 4 steps
  </verify>
  <done>
- All 4 step components render their questions
- QuestionStep handles all field types (text, select, multi-select, radio, yes-no)
- Navigation between steps works (forward with validation, backward without)
- Form values preserved when navigating back
- Step registry exports steps array and getStepSchema function
  </done>
</task>

<task type="auto">
  <name>Task 3: Update sidebar navigation</name>
  <files>components/ui/Sidebar.tsx</files>
  <action>
Update the existing Sidebar.tsx to enable the Assessment link.

Current state (from 01-04 summary): Assessment nav item exists but is disabled with "Soon" badge.

Changes:
- Remove "disabled" state from Assessment link
- Remove "Soon" badge
- Make it a working link to /assessment
- Add active state styling when on /assessment route
- Keep same visual style as Platforms link

Use Next.js usePathname() to detect active route for styling.
  </action>
  <verify>
1. Click Assessment in sidebar - navigates to /assessment
2. Assessment link shows active styling when on /assessment
3. Platforms link no longer shows active when on /assessment
  </verify>
  <done>
- Assessment link in sidebar is clickable and navigates to /assessment
- Active state styling applied correctly
- No "Soon" badge or disabled styling
  </done>
</task>

</tasks>

<verification>
1. Navigate to /assessment from sidebar
2. Complete all 4 steps filling in values
3. Navigate backward and verify values preserved
4. Step indicator updates correctly at each step
5. Validation errors appear when trying to proceed with invalid/empty required fields
6. `npm run build` succeeds
</verification>

<success_criteria>
- /assessment page loads and renders form
- 4 steps with appropriate questions each
- Forward navigation validates, backward navigation doesn't
- Step indicator shows progress accurately
- Sidebar Assessment link works
- All field types render correctly
- Form values preserved during navigation
</success_criteria>

<output>
After completion, create `.planning/phases/02-assessment-engine/02-02-SUMMARY.md`
</output>
