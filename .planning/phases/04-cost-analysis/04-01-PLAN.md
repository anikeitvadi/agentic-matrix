---
phase: 04-cost-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - lib/cost/types.ts
  - velite.config.ts
  - content/platforms/anthropic-claude.mdx
  - content/platforms/openai-frontier.mdx
  - content/platforms/salesforce-agentforce.mdx
  - content/platforms/microsoft-copilot-studio.mdx
  - content/platforms/tray-ai.mdx
autonomous: true

must_haves:
  truths:
    - "Cost types exist for all four pricing models (token, subscription, per-conversation, hybrid)"
    - "Platform MDX files have structured pricing data that calculators can consume"
    - "Required dependencies (recharts, slider) are installed and importable"
  artifacts:
    - path: "lib/cost/types.ts"
      provides: "CostEstimate, PricingModel, UsageParameters, EngineeringEstimate types"
      min_lines: 80
    - path: "velite.config.ts"
      provides: "Extended pricing schema with tokenPricing, tiers, perConversationRate"
      contains: "tokenPricing"
  key_links:
    - from: "lib/cost/types.ts"
      to: ".velite/index.ts"
      via: "Platform type import for pricing data"
      pattern: "import.*Platform.*velite"
---

<objective>
Set up the cost analysis foundation: install dependencies, define cost calculation types, and extend the platform pricing schema.

Purpose: Create the type contracts and data structures that all cost calculators and UI components will depend on. Without structured pricing data in MDX and well-defined types, the calculators cannot be implemented correctly.

Output: lib/cost/types.ts with full type definitions, extended velite pricing schema, and 5 platform MDX files updated with structured pricing data.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cost-analysis/04-RESEARCH.md

# Existing patterns to follow
@lib/scoring/types.ts
@velite.config.ts
@content/platforms/anthropic-claude.mdx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and create cost types</name>
  <files>package.json, lib/cost/types.ts</files>
  <action>
Install required dependencies:
```bash
npm install recharts @radix-ui/react-slider
```

Create lib/cost/types.ts with comprehensive type definitions following the patterns in lib/scoring/types.ts. Include:

1. PricingModel enum: 'token-based' | 'subscription' | 'per-conversation' | 'hybrid'

2. TokenPricing interface:
   - inputPricePerMillion: number
   - outputPricePerMillion: number
   - cachedInputDiscount?: number
   - modelVariants?: array of {name, inputPrice, outputPrice}

3. SubscriptionTier interface:
   - name: string
   - monthlyPrice: number
   - includedUnits?: number
   - unitType?: 'conversations' | 'users' | 'tasks' | 'tokens'

4. PricingData interface (matches velite schema):
   - model: PricingModel
   - details: string
   - tokenPricing?: TokenPricing
   - tiers?: SubscriptionTier[]
   - perConversationRate?: number
   - includedConversations?: number
   - enterpriseContact?: boolean

5. UsageParameters interface:
   - monthlyConversations: number
   - monthlyInputTokens: number
   - monthlyOutputTokens: number
   - avgTokensPerConversation?: number

6. CostBreakdown interface:
   - category: 'platform' | 'token' | 'infrastructure' | 'personnel'
   - item: string
   - monthlyCost: number
   - annualCost: number
   - notes?: string

7. CostEstimate interface:
   - platformId: string
   - platformName: string
   - pricingModel: PricingModel
   - monthlyUsageCost: number
   - monthlyBaseCost: number
   - monthlyTotal: number
   - yearlyTotal: number
   - engineeringDays: number
   - engineeringCost: number
   - tcoPeriods: { months12, months24, months36 }
   - breakdown: CostBreakdown[]

8. EngineeringEstimate interface:
   - optimisticDays: number
   - mostLikelyDays: number
   - pessimisticDays: number
   - expectedDays: number
   - confidenceRange: { low: number, high: number }

9. PlatformComplexity interface:
   - tier: 'enterprise-os' | 'ipaas-agent' | 'developer-first' | 'vertical'
   - hasNativeIntegration: boolean
   - requiresCustomCode: boolean
   - complianceRequirements: string[]

Add JSDoc comments following the style in lib/scoring/types.ts.
  </action>
  <verify>
Run: `npm run build` - should compile without type errors
Check: `ls node_modules/recharts node_modules/@radix-ui/react-slider` - both exist
  </verify>
  <done>
lib/cost/types.ts exports all 9 interfaces, recharts and slider packages are installed
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend velite pricing schema</name>
  <files>velite.config.ts</files>
  <action>
Update the pricing schema in velite.config.ts to include structured pricing data.

Current schema:
```typescript
pricing: s.object({
  model: s.string(),
  details: s.string(),
}),
```

New schema:
```typescript
pricing: s.object({
  model: s.enum(['pay-per-use', 'subscription', 'per-conversation', 'hybrid']),
  details: s.string(),

  // Token pricing (for pay-per-use and hybrid models)
  tokenPricing: s.object({
    inputPricePerMillion: s.number(),
    outputPricePerMillion: s.number(),
    cachedInputDiscount: s.number().optional(),
    modelVariants: s.array(s.object({
      name: s.string(),
      inputPrice: s.number(),
      outputPrice: s.number(),
    })).optional(),
  }).optional(),

  // Subscription tiers
  tiers: s.array(s.object({
    name: s.string(),
    monthlyPrice: s.number(),
    includedUnits: s.number().optional(),
    unitType: s.enum(['conversations', 'users', 'tasks', 'tokens']).optional(),
  })).optional(),

  // Per-conversation pricing
  perConversationRate: s.number().optional(),
  includedConversations: s.number().optional(),

  // Flags
  enterpriseContact: s.boolean().optional(),
  infrastructureCosts: s.string().optional(),
}),
```

Note: The model enum changes from free-form string to constrained enum. Existing MDX files may need updates to match.
  </action>
  <verify>
Run: `npm run dev` and check that velite compiles without schema validation errors
Check that .velite/index.ts regenerates with new pricing types
  </verify>
  <done>
velite.config.ts has extended pricing schema with all four pricing model support
  </done>
</task>

<task type="auto">
  <name>Task 3: Update platform MDX with structured pricing</name>
  <files>
    content/platforms/anthropic-claude.mdx
    content/platforms/openai-frontier.mdx
    content/platforms/salesforce-agentforce.mdx
    content/platforms/microsoft-copilot-studio.mdx
    content/platforms/tray-ai.mdx
  </files>
  <action>
Update 5 representative platform MDX files with structured pricing data. These cover all 4 pricing models.

**anthropic-claude.mdx** (pay-per-use):
```yaml
pricing:
  model: pay-per-use
  details: Token-based with API credits, enterprise plans available
  tokenPricing:
    inputPricePerMillion: 3
    outputPricePerMillion: 15
    modelVariants:
      - name: Claude 3.5 Sonnet
        inputPrice: 3
        outputPrice: 15
      - name: Claude Opus 4.5
        inputPrice: 5
        outputPrice: 25
  enterpriseContact: true
```

**openai-frontier.mdx** (pay-per-use):
```yaml
pricing:
  model: pay-per-use
  details: Token-based API pricing, volume discounts available
  tokenPricing:
    inputPricePerMillion: 2.5
    outputPricePerMillion: 10
    modelVariants:
      - name: GPT-4o
        inputPrice: 2.5
        outputPrice: 10
      - name: GPT-4.5 Turbo
        inputPrice: 5
        outputPrice: 15
  enterpriseContact: true
```

**salesforce-agentforce.mdx** (per-conversation):
```yaml
pricing:
  model: per-conversation
  details: $2 per conversation, included conversations vary by Salesforce edition
  perConversationRate: 2
  tiers:
    - name: Starter
      monthlyPrice: 0
      includedUnits: 1000
      unitType: conversations
    - name: Professional
      monthlyPrice: 150
      includedUnits: 5000
      unitType: conversations
    - name: Enterprise
      monthlyPrice: 300
      includedUnits: 10000
      unitType: conversations
  enterpriseContact: true
```

**microsoft-copilot-studio.mdx** (hybrid):
```yaml
pricing:
  model: hybrid
  details: Base subscription plus per-message usage
  tokenPricing:
    inputPricePerMillion: 1
    outputPricePerMillion: 4
  tiers:
    - name: Base
      monthlyPrice: 200
      includedUnits: 25000
      unitType: conversations
  enterpriseContact: true
```

**tray-ai.mdx** (subscription):
```yaml
pricing:
  model: subscription
  details: Tiered plans based on tasks and connectors
  tiers:
    - name: Professional
      monthlyPrice: 995
      includedUnits: 5000
      unitType: tasks
    - name: Enterprise
      monthlyPrice: 3495
      includedUnits: 25000
      unitType: tasks
  enterpriseContact: true
```

Keep the rest of the MDX frontmatter (slug, title, description, tier, capabilities, officialDocs, pricingPage, lastVerified) and body content unchanged.
  </action>
  <verify>
Run: `npm run dev` - velite should compile all platforms without errors
Check: No validation errors in terminal output
  </verify>
  <done>
5 platform MDX files have structured pricing data covering all 4 pricing models
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `ls lib/cost/` shows types.ts
3. `grep "tokenPricing" velite.config.ts` returns a match
4. `grep "pay-per-use" content/platforms/anthropic-claude.mdx` returns a match
5. .velite/index.ts contains extended Platform type with pricing fields
</verification>

<success_criteria>
- lib/cost/types.ts exports all cost-related types (9 interfaces)
- recharts and @radix-ui/react-slider are in package.json dependencies
- velite.config.ts has extended pricing schema with enum model type
- 5 platform MDX files have structured pricing data
- Project builds and compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-cost-analysis/04-01-SUMMARY.md`
</output>
